"use strict";
/*
//Coding Challenge #1
const calcAverage = (a, b, c) => (a + b + c) / 3;
let averageDolph = calcAverage(85, 54, 41);
let averageKoal = calcAverage(23, 34, 27);
function checkWinner(p1, p2) {
  if (p1 >= 2 * p2) {
    console.log(`Team Dolphin wins!(${p1} vs ${p2})`);
  } else if (p2 >= 2 * p1) {
    console.log(`Team Koalas wins!(${p2} vs ${p1})`);
  }
}

checkWinner(averageDolph, averageKoal);

//Coding Challenge #2
let bill;
bill = 100;
let tip;
if (bill <= 300 && bill >= 50) {
  tip = bill * 0.15;
} else {
  tip = bill * 0.2;
}
console.log(
  `For a bill of ${bill}, the tip is ${tip} hence total value is ${bill + tip}.`
);

const bills = [125, 555, 44];
const tips = [];
const totals = [];
for (let i = 0; i < bills.length; i++) {
  const bill = bills[i];
  if (bill <= 300 && bill >= 50) {
    tip = bill * 0.15;
  } else {
    tip = bill * 0.2;
  }
  tips.push(tip);
  totals.push(tip + bill);
}
console.log(tips);
console.log(totals);
*/

//Coding Challenge #2
